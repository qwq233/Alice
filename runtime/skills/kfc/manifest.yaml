name: kfc
version: "1.0.0"
author: "Alice"
license: "MIT"
description: "Crazy Thursday — KFC V50 copypasta meme"

actions:
  - name: use_kfc_app
    category: fun
    description:
      - "Get a Crazy Thursday (疯狂星期四) copypasta — the legendary KFC meme."
    usageHint: "Thursday meme. Best used on Thursdays for maximum cultural impact."
    whenToUse: "On Thursdays, or when someone mentions KFC / V50 / 疯狂星期四"
    whenNotToUse: "When food memes aren't relevant"
    params: []
    returns: "JSON result in next round."

runtime:
  backend: shell
  timeout: 10
  shell:
    command: "npx tsx bin/kfc.ts"
