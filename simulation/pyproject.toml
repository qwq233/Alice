[project]
name = "companion-pressure-sim"
version = "0.1.0"
description = "Simulation study for digital companion pressure-field dynamics"
requires-python = ">=3.13"
dependencies = [
    "networkx>=3.0",
    "numpy>=1.24",
    "scipy>=1.10",
    "matplotlib>=3.7",
]
